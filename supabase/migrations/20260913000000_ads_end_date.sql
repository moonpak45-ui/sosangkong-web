-- ============================================================
-- 광고 종료일(end_date) 추가 — box/line 실제 노출 작업의 선행 스키마 변경
-- 대상 테이블: ads
-- Supabase SQL Editor에 그대로 붙여넣어 실행하세요. (재실행해도 안전 —
-- add column if not exists)
--
-- 배경) box(박스광고)/line(줄광고)를 BuyerHomeFeed에 실제로 노출하는 작업
-- 지시에 "end_date 지난 건 자동으로 노출 제외"가 명시적으로 포함돼 있었지만,
-- 20260912000000_ads_system.sql 설계 당시엔(그땐 banner만 실제 노출
-- 대상이었고 만료 개념 자체를 이번 범위 밖으로 정했었음) 이 컬럼이 없었음.
-- 이번에 추가.
--
-- 종료일은 공급업체가 신청 시점에 직접 입력(선택, 비워두면 무기한 — 기존
-- 배너의 "종료일 없음 = 계속 노출" 기본 동작과 동일하게 유지). 관리자가
-- 승인 화면에서 종료일을 조정하는 UI는 이번에 만들지 않음(필요해지면
-- 다음에 추가 — HANDOFF 참고).
--
-- banner(롤링배너)도 같은 필터를 적용하도록 화면(components/
-- AdRollingBanner.tsx)을 함께 고쳤음 — 기존 배너 신청 행은 전부 end_date가
-- null이라 동작 변화 없음(계속 무기한 노출), 앞으로 배너도 종료일을
-- 지정할 수 있게 된 것뿐. RLS는 변경 없음(이 프로젝트 관례상 RLS는 행 단위
-- 제어라 컬럼 추가만으로는 정책을 안 건드려도 됨 — partner는 이미
-- ads_insert_own으로 자기 신청 행의 모든 컬럼을 with check 통과 조건 안에서
-- 자유롭게 채울 수 있음).
-- ============================================================

alter table ads add column if not exists end_date date;
